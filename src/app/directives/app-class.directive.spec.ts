import { AppClassDirective } from './app-class.directive';

describe('AppClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AppClassDirective();
    expect(directive).toBeTruthy();
  });
});
